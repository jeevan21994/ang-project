import { FilterMobPipe } from './filter-mob.pipe';

describe('FilterMobPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMobPipe();
    expect(pipe).toBeTruthy();
  });
});
